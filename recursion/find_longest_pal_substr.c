#include "stdio.h"
#include <vector>


void
find_longest_pal_substr(char *str, int len)
{
    int i = 0;
    for (i=0;;i<len;i++) {
        l = i;
        r = i
        while ((str[l] == str[r]) && 
                (l >= 0) && (r < len)) {
           
	}
    } 
}

int main()
{
    printf("Hello World\n");
}
